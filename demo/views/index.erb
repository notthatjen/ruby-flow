<!DOCTYPE html>
<html>
<head>
  <title>RubyFlow Demo</title>
  <link rel="icon" type="image/svg+xml" href="/images/ruby-flow-icon.svg">
  <style>
    body { 
      margin: 0;
      font-family: sans-serif;
      overflow: hidden;
    }
    #flow-container {
      width: 100vw;
      height: 100vh;
      background-color: #f8f9fa;
    }
    .node {
      fill: white;
      stroke: #555;
      stroke-width: 2px;
      cursor: move;
    }
    .node-label {
      text-anchor: middle;
      dominant-baseline: middle;
      pointer-events: none;
      font-size: 14px;
    }
    .edge {
      stroke: #555;
      stroke-width: 2px;
      marker-end: url(#arrowhead);
    }
    .controls {
      position: absolute;
      bottom: 20px;
      right: 20px;
      background: white;
      padding: 10px;
      border-radius: 5px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }
    button {
      margin: 0 5px;
      padding: 5px 10px;
    }
  </style>
</head>
<body>
  <%= ruby_flow_render(@graph) %>
  <script src="/js/flow.js"></script>
</body>
</html> 